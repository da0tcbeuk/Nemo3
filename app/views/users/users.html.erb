<h1>ユーザーマイページ</h1>
<p>ユーザーネーム : <%= @user.name %></p>
<p>プロフィール画像 :<%=@deu.image %></p>
<p>メールアドレス : <%= @user.email %></p>
<p>プロフィール : <%= @user.profile %></p>

<% if current_user.id == @user.id %>
  <%= link_to "編集する", edit_user_registrations_path %>
<% end %>

<h2>ユーザーの投稿一覧</h2>
<% @user.deus.each do |t| %>
  <%= t.Title %>
<% end %>

h2>ユーザーがいいねした投稿一覧</h2>
<% @user.liked_deus.each do |t| %>
  <%= t.user.name %>
  <%= t.Title %>
<% end %>

<%= link_to "記事一覧に戻る", deus_path %>